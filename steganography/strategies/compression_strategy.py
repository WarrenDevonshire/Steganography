

def hide_data(carry, data, seed):
    """Hide data in carry and return a numpy array.

    Keyword arguments:
    carry -- the one dimensional array to hide data in
    data -- an array of bits to hide
    """
